import React from 'react'
import { Darkbuttons, Lightbuttons } from '@/lib/Data/techstackdata'

export default function stack() {
    const stackmap = {const theme === "dark" ? Darkbuttons.map((item) =>  (
    )) : Lightbuttons.map((item) => {
        
    })}

  return (
    <div>stack</div>
  )
}
